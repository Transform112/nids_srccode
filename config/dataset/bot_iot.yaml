# Degenerate case: NF-BoT-IoT-v3
# 20 source IPs and 99.69% attack prevalence. Used only for explicit negative result.
data:
  dataset: bot_iot
  node_granularity: ip_port
  subsample_target: 1000000  # docs/02_DATASETS.md §5.3 override table
graph:
  node_granularity: ip_port

classes:
  benign: Benign
  attacks:
    - DoS
    - DDoS
    - Reconnaissance
    - Theft
