# Tertiary dataset: NF-UNSW-NB15-v3
# Only 40 unique source IPs: IP-level graphs degenerate, so force ip_port nodes.
data:
  dataset: unsw_nb15
  node_granularity: ip_port
  subsample_target: null  # "full" per docs/02_DATASETS.md §5.3 — no subsampling
graph:
  node_granularity: ip_port

classes:
  benign: Benign
  attacks:
    - Exploits
    - Fuzzers
    - Generic
    - Reconnaissance
    - DoS
    - Backdoor
    - Shellcode
    - Analysis
    - Worms
